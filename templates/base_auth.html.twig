<!DOCTYPE html>
<html lang="fr">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Application Symfony{% endblock %}</title>
    <link rel="icon"
        href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text><text y=%221.3em%22 x=%220.2em%22 font-size=%2276%22 fill=%22%23fff%22>sf</text></svg>">
    {% block stylesheets %}{% endblock %}
    {% block importmap %}{{ importmap('app') }}{% endblock %}
</head>

<body class="auth-page {% block page_class %}{% endblock %}">
    <!-- Navigation simplifiée -->
    <nav class="navbar">
        <div class="container">
            <div class="nav-content">
                <a href="{{ path('app_home') }}" class="nav-brand">
                    <span>🚀</span> Mon App
                </a>
                <div class="nav-links">
                    <a href="{{ path('app_home') }}" class="nav-link">← Retour à l'accueil</a>
                </div>
            </div>
        </div>
    </nav>

    <!-- Main Content -->
    <main class="main-content">
        <div class="container">
            {% block body %}{% endblock %}
        </div>
    </main>

    <!-- Footer simplifié -->
    <footer class="footer">
        <div class="container">
            <div class="text-center">
                <p class="text-secondary">&copy; {{ 'now'|date('Y') }} Mon App. Développé avec Symfony</p>
            </div>
        </div>
    </footer>

    {% block javascripts %}{% endblock %}
</body>

</html>