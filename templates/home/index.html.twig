{% extends 'base.html.twig' %}

{% block title %}Accueil - Application Symfony{% endblock %}

{% block body %}

    <h1>🚀 Bienvenue sur Mon App</h1>

<div class="welcome-features">
    <div class="card feature-card">
        <div class="feature-icon">🔐</div>
        <div class="card-body">
            <h3 class="card-title">Authentification</h3>
            <p>Système d'authentification sécurisé avec inscription et connexion</p>
            {% if not app.user %}
            <div class="d-flex gap-2 justify-center mt-3">
                <a href="{{ path('app_login') }}" class="btn btn-primary">Se connecter</a>
                <a href="{{ path('app_register') }}" class="btn btn-outline">S'inscrire</a>
            </div>
            {% endif %}
        </div>
    </div>

    <div class="card feature-card">
        <div class="feature-icon">👥</div>
        <div class="card-body">
            <h3 class="card-title">Gestion d'utilisateurs</h3>
            <p>Interface CRUD complète pour la gestion des utilisateurs</p>
            {% if app.user %}
            <div class="mt-3">
                <a href="{{ path('app_user_index') }}" class="btn btn-primary">Voir les utilisateurs</a>
            </div>
            {% endif %}
        </div>
    </div>

    <div class="card feature-card">
        <div class="feature-icon">⚡</div>
        <div class="card-body">
            <h3 class="card-title">Performance</h3>
            <p>Application optimisée avec cache, assets compilés et déploiement automatisé</p>
        </div>
    </div>
</div>

{% if app.user %}
<div class="card mt-4">
    <div class="card-header">
        <h2 class="card-title">Tableau de bord</h2>
    </div>
    <div class="card-body">
        <div class="user-status">
            <div class="user-avatar">{{ app.user.email|first|upper }}</div>
            <div>
                <p><strong>Connecté en tant que :</strong> {{ app.user.email }}</p>
                <p class="text-secondary">Rôles : {{ app.user.roles|join(', ') }}</p>
            </div>
        </div>
    </div>
</div>
{% endif %}
{% endblock %}